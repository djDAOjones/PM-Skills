# Wish-list — pm-skills framework repository

<!-- Capture inbox for unscoped ideas. Cold tier — read only at triage
     (session-start Start B, or when the size check flags it).
     Promote into backlog.md or cut; no history kept here. -->

## Open

- CLOUD-TRUTH: retire the "unsupported" claim in the AGENTS
  template's hostile-filesystem guard — every deployment on record,
  this repository included, lives on a cloud-synced path. State the
  observed failure modes and standing mitigations instead, and add
  the operational cloud-sync section to the DEV-INFRASTRUCTURE
  template, where both coding projects independently hand-wrote
  theirs. (source: field study run two, FS2-06 — the named
  retirement; **promotion-ready**: two template files + release)
- BUDGET-QUALITY-BAR: `memory-policy.md` states the quality-bar
  stop the field already runs — live context feeding open work
  outranks prune-to arithmetic, the stop is recorded, count budgets
  stay hard. Both projects invented this doctrine independently;
  the Active-over-budget warning stood in ten of twelve Video
  Helper sessions. (source: field study run two, FS2-03)
- RULEBOOK-BUDGET: populated rulebooks join the reference-doc soft
  budgets so the validator warns on rulebook growth — a fresh init
  keeps every template section and only adds (4,502 words in four
  days at the Video Helper; 993 suffices at Route Plotter); plus
  one `init.md` line saying template sections are a menu. (source:
  field study run two, FS2-04)
- REVIEW-SILENCE-WATCH: standing observable, no edit now — if the
  next two field reports show `findings.md` firing while
  `review.md` stays silent, shrink `review.md` to the entry point
  that routes to the posture and the verify half. review.md fired
  zero times across two projects that each ran a review round.
  (source: field study run two, FS2-02; trigger-shaped)
- SESSION-CHECKPOINT: `end-of-task.md` names the mid-task
  checkpoint commit — the lite close's first real job (`Close:
  lite`: zero firings in 197 field commits) — so sessions stop
  ending with work uncommitted on synced checkouts; watch the
  trailer for retirement if still unused after. (source: field
  study run two, FS2-05)
- WAVES-PILOT: run `prompts/findings.md` + `improvement-waves.md`
  end-to-end against a real consuming project (Route Plotter or UoN
  Video Helper) and file the result as a field report. Both prompts
  shipped 2026-08-27 unvalidated in use — this repository has no
  application source to pilot them on, and both tickets named that
  gap. One run closes it for both.
- ROADMAP-DIST: distribute the roadmap renderer — `gen-roadmap.mjs`
  to `pm_skills/scaffold/`, the view to the project-memory templates —
  once this repo has actually used the source-only copy for a while.
  Held deliberately at BACKLOG-TABLE's close (2026-08-27): shipping it
  now doubles the maintenance under the deliberate-fork rule for a
  shape with no usage evidence.
- NEXT-FRAGMENTS: pm-next v0.2 — per-item decision/trajectory
  fragments folded at integration, closing the successor's last
  shared-append residue. (source: reflection run one; PACE-POLICY
  adopted 2026-08-17 — paused until the Hub leg lands, then route
  to the lab's own queue)
